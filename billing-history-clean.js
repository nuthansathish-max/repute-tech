// Disabled: this module previously wrapped Express middleware and could interfere
// with Billing & POS API responses. Bill History UI is handled directly by the
// Billing & POS page module instead.
export {};
