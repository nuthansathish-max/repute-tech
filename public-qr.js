import express from 'express';
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

function esc(value) {
  return String(value ?? '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
}

function publicHubHtml({ business, menus, slug }) {
  const firstMenu = menus[0];
  const firstOrderHref = firstMenu ? `/q/${encodeURIComponent(slug)}/order/${encodeURIComponent(firstMenu.id)}` : `/q/${encodeURIComponent(slug)}/order`;
  const previewItems = menus.flatMap(menu => (menu.items || []).slice(0, 3).map(item => ({...item, menuName: menu.name, menuId: menu.id})));
  const menuHtml = menus.length ? menus.map(menu => {
    const menuOrderHref = `/q/${encodeURIComponent(slug)}/order/${encodeURIComponent(menu.id)}`;
    return `<section class="menu"><div class="menuHead"><div><div class="eyebrow dark">MENU</div><h2>${esc(menu.name)}</h2></div><a class="smallBuy" href="${menuOrderHref}">Order this menu</a></div>${(menu.items||[]).length ? menu.items.map(item => `<div class="item"><div class="itemInfo"><strong>${esc(item.name)}</strong>${item.description?`<p>${esc(item.description)}</p>`:''}</div>${item.price!==null&&item.price!==undefined&&item.price!==''?`<b class="price">₹${esc(item.price)}</b>`:''}</div>`).join('') : '<p class="muted">No items available yet.</p>'}</section>`;
  }).join('') : '<section class="empty">No published menus are available yet.</section>';
  const previewHtml = previewItems.length ? `<section class="shop"><div class="sectionTitle"><div><span>Popular picks</span><h2>Products & ordering</h2></div><a href="${firstOrderHref}">Shop first menu →</a></div><div class="productGrid">${previewItems.map(item => { const href=`/q/${encodeURIComponent(slug)}/order/${encodeURIComponent(item.menuId)}`; return `<article class="product"><div class="productIcon">🍽️</div><div class="productBody"><strong>${esc(item.name)}</strong><small>${esc(item.menuName)}</small><b>₹${esc(item.price)}</b></div><a class="buy" href="${href}">Buy</a></article>`; }).join('')}</div></section>` : '';
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#5b4bdb"><title>${esc(business.name)} — Customer Hub</title><style>*{box-sizing:border-box}body{margin:0;background:#f6f7fb;color:#171725;font-family:Inter,Arial,system-ui,sans-serif}.wrap{max-width:760px;margin:auto;padding:14px 14px 40px}.hero{background:linear-gradient(135deg,#4c3bb8,#7b68ee);color:white;border-radius:24px;padding:26px 20px;margin-bottom:14px;box-shadow:0 12px 30px #4034a526}.eyebrow{font-size:11px;text-transform:uppercase;letter-spacing:.13em;opacity:.78}.eyebrow.dark{color:#5b4bdb;opacity:1}.hero h1{margin:7px 0;font-size:29px;letter-spacing:-.02em}.hero p{margin:0;opacity:.9;line-height:1.5}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:14px}.actions a,.actions button{border:0;border-radius:15px;background:white;color:#4034a5;padding:14px 10px;font-weight:800;text-align:center;text-decoration:none;box-shadow:0 5px 16px #0000000d}.actions .order{background:#171725;color:#fff}.shop,.menu,.empty{background:white;border-radius:20px;padding:18px;margin:12px 0;box-shadow:0 5px 18px #0000000b}.sectionTitle,.menuHead{display:flex;justify-content:space-between;align-items:center;gap:12px}.sectionTitle span{font-size:11px;color:#667085;text-transform:uppercase;letter-spacing:.1em}.sectionTitle h2,.menu h2{margin:3px 0 12px}.sectionTitle a,.smallBuy{color:#4c3bb8;font-weight:800;text-decoration:none;font-size:13px}.productGrid{display:grid;gap:9px}.product{display:flex;align-items:center;gap:10px;padding:11px;border:1px solid #ececf2;border-radius:15px}.productIcon{width:44px;height:44px;border-radius:12px;background:#f0edff;display:grid;place-items:center;font-size:21px}.productBody{display:flex;flex-direction:column;gap:2px;flex:1;min-width:0}.productBody strong{font-size:14px}.productBody small{color:#888;font-size:11px}.productBody b{font-size:13px;margin-top:2px}.buy{background:#5b4bdb;color:white;border-radius:10px;padding:9px 12px;text-decoration:none;font-size:12px;font-weight:800}.menuHead{margin-bottom:3px}.item{display:flex;justify-content:space-between;gap:12px;padding:13px 0;border-top:1px solid #eee}.item:first-of-type{border-top:0}.itemInfo{min-width:0}.item p{margin:4px 0 0;color:#666;font-size:13px;line-height:1.4}.price{white-space:nowrap}.muted{color:#777}.footer{text-align:center;color:#888;font-size:12px;padding:20px}.modal{display:none;position:fixed;inset:0;background:#0008;align-items:flex-end;padding:10px;z-index:20}.modal.open{display:flex}.sheet{background:white;border-radius:24px;padding:20px;width:min(520px,100%);margin:auto;box-shadow:0 15px 50px #0005}.sheet h2{margin:2px 0 5px}.sheet .sub{color:#667085;font-size:13px;margin:0 0 16px}.stars{display:grid;grid-template-columns:repeat(5,1fr);gap:6px;margin:8px 0 10px}.starChoice{border:1px solid #e4e5eb;background:#fff;border-radius:14px;padding:10px 4px;cursor:pointer;text-align:center}.starChoice .star{display:block;font-size:25px;line-height:1.1}.starChoice .label{display:block;font-size:9px;font-weight:800;color:#667085;margin-top:4px}.starChoice.selected{border-color:#5b4bdb;background:#f0edff;box-shadow:0 0 0 2px #5b4bdb22}.starChoice.selected .label{color:#4c3bb8}.ratingHint{text-align:center;font-weight:800;color:#4034a5;min-height:20px;margin-bottom:8px}textarea,input{width:100%;padding:12px;border:1px solid #ddd;border-radius:12px;margin:6px 0 10px;font:inherit}.sheet button.submit{width:100%;padding:13px;border:0;border-radius:12px;background:#5b4bdb;color:white;font-weight:800}.sheet button.close{width:100%;padding:12px;border:0;border-radius:12px;background:#eee;color:#333;margin-top:8px;font-weight:700}.status{font-size:13px;text-align:center;margin:10px 0 0}.successBox{text-align:center;background:#f0fdf4;border:1px solid #bbf7d0;border-radius:16px;padding:18px;color:#166534}.successBox .big{font-size:34px}.privacy{font-size:11px;color:#888;line-height:1.4;margin-top:8px;text-align:center}@media(max-width:480px){.stars{gap:4px}.starChoice{padding:9px 2px}.starChoice .star{font-size:22px}.starChoice .label{font-size:8px}}</style></head><body><div class="wrap"><header class="hero"><div class="eyebrow">Customer Hub</div><h1>${esc(business.name)}</h1><p>Browse our products, order easily, and share your experience.</p></header><div class="actions"><a href="#menus">🍽️ View Menu</a><a class="order" href="${firstOrderHref}">🛍️ Order ${firstMenu ? esc(firstMenu.name) : 'Products'}</a><button onclick="openFeedback()">⭐ Rate Experience</button></div>${previewHtml}<main id="menus">${menuHtml}</main><div class="footer">Powered by repute-tech.in</div></div><div class="modal" id="feedback" onclick="if(event.target===this)closeFeedback()"><div class="sheet" id="sheet"><h2>How was your experience?</h2><p class="sub">Your feedback helps us serve you better.</p><div class="stars">${[[5,'Excellent'],[4,'Very good'],[3,'Good'],[2,'Fair'],[1,'Poor']].map(([n,label])=>`<button type="button" class="starChoice" data-rating="${n}" onclick="chooseRating(${n})"><span class="star">★</span><span class="label">${label}</span></button>`).join('')}</div><div class="ratingHint" id="ratingHint">Select a rating</div><input id="authorName" maxlength="80" placeholder="Your name (optional)"><textarea id="message" rows="4" placeholder="Tell us about your experience (optional)"></textarea><button class="submit" id="submitBtn" onclick="submitFeedback()">Submit Review</button><button class="close" onclick="closeFeedback()">Cancel</button><p class="status" id="status"></p><p class="privacy">No account is required. Your review is sent to the business review inbox.</p></div></div><script>const businessId=${JSON.stringify(business.id)};let selectedRating=0;const labels={1:'Poor — we can do better',2:'Fair — room for improvement',3:'Good — thanks for sharing',4:'Very good — glad you enjoyed it',5:'Excellent — thank you!'};function openFeedback(){document.getElementById('feedback').classList.add('open');document.getElementById('status').textContent=''}function closeFeedback(){document.getElementById('feedback').classList.remove('open')}function chooseRating(n){selectedRating=n;document.querySelectorAll('.starChoice').forEach(b=>b.classList.toggle('selected',Number(b.dataset.rating)===n));document.getElementById('ratingHint').textContent=labels[n]}async function submitFeedback(){const status=document.getElementById('status');const btn=document.getElementById('submitBtn');if(!selectedRating){status.textContent='Please select a star rating first.';status.style.color='#b42318';return}btn.disabled=true;status.textContent='Submitting your review…';status.style.color='#667085';try{const r=await fetch('/api/customer/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({businessId,rating:selectedRating,message:document.getElementById('message').value,authorName:document.getElementById('authorName').value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to submit review');document.getElementById('sheet').innerHTML='<div class="successBox"><div class="big">⭐</div><h2>Thank you!</h2><p>Your '+selectedRating+'-star review has been received by the business.</p><button class="submit" onclick="closeFeedback()">Done</button></div>';}catch(e){status.textContent=e.message||'Unable to submit review';status.style.color='#b42318';btn.disabled=false}}</script></body></html>`;
}

async function servePublicQr(req,res,next){
  try{
    const slug=String(req.params.slug||'').trim();
    if(!slug)return res.status(404).send('QR code not found');
    const qr=await prisma.smartQr.findFirst({where:{slug},include:{business:{include:{menus:{where:{isPublished:true},orderBy:{createdAt:'desc'},include:{items:{orderBy:{name:'asc'}}}}}}}});
    if(!qr?.business)return res.status(404).send('QR code not found');
    await prisma.smartQr.update({where:{id:qr.id},data:{scanCount:{increment:1}}}).catch(()=>{});
    return res.type('html').send(publicHubHtml({business:qr.business,menus:qr.business.menus||[],slug:qr.slug}));
  }catch(error){return next(error);}
}

function registerPublicQr(app){
  if(app.__publicQrRegistered)return;
  app.__publicQrRegistered=true;
  for(const path of ['/q/:slug','/public/qr/:slug','/public/q/qr/:slug']) app.get(path,servePublicQr);
}

const previousGet=express.application.get;
if(!express.application.__publicQrGetPatched){
  express.application.__publicQrGetPatched=true;
  express.application.get=function patchedPublicQrGet(path,...handlers){
    if(!this.__publicQrRegistering && !this.__publicQrRegistered){
      this.__publicQrRegistering=true;
      registerPublicQr(this);
      this.__publicQrRegistering=false;
    }
    return previousGet.call(this,path,...handlers);
  };
}
